{
  "name": "sage",
  "version": "0.1.0",
  "description": "Sage — botanical-named code review agent on pi.dev substrate, speaks Myelin envelopes",
  "type": "module",
  "module": "src/cli/index.ts",
  "bin": {
    "sage": "./src/cli/index.ts"
  },
  "scripts": {
    "sage": "bun run src/cli/index.ts",
    "review": "bun run src/cli/index.ts review",
    "serve": "bun run src/cli/index.ts serve",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "nats": "^2.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
